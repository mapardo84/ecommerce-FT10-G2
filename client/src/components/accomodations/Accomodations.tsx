import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Accomodations({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

